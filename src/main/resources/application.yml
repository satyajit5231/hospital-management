server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hospital_db
    username: postgres
    password: ${DB_PASSWORD:Satya22}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  application:
    name: hospital-management

jwt:
  secret: satyajeet_hospital_jwt_secret_key_2025_very_long_secure_key
  expiration: 86400000

logging:
  level:
    com.satyajeet: INFO
    org.springframework.security: WARN
